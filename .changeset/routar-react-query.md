---
"@routar/react-query": minor
"@routar/core": minor
---

Add `@routar/react-query`: derive TanStack Query `queryOptions`/`mutationOptions` factories from a routar API client + router via `createQueries`, with declarative invalidation through `routarMutationCache`. `@routar/core`'s `endpoint()` now preserves the literal `method` type and exports the `ApiClient` type.
