/**
 * @deprecated `createFetchExecutor` has moved to `@routar/core`.
 * Import it from there instead:
 * ```ts
 * import { createFetchExecutor } from '@routar/core';
 * ```
 */
export { createFetchExecutor } from "@routar/core";

/**
 * @deprecated `HttpError` has moved to `@routar/core`.
 * Import it from there instead:
 * ```ts
 * import { HttpError } from '@routar/core';
 * ```
 */
export { HttpError } from "@routar/core";
