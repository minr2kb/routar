{
  "name": "@routar/root",
  "private": true,
  "workspaces": ["packages/*", "apps/*"],
  "scripts": {
    "build": "bun run --filter '@routar/core' build && bun run --filter '@routar/fetch' build && bun run --filter '@routar/axios' build && bun run --filter '@routar/web' build && bun run --filter '@routar/docs' build",
    "test": "bun test",
    "typecheck": "tsc --build"
  },
  "dependencies": {
    "typescript": "^6.0.3"
  },
  "devDependencies": {
    "@changesets/cli": "^2.31.0",
    "@types/bun": "^1.3.14"
  }
}
